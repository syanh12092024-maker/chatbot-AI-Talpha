node_modules/
.env
*.log

tokens.json
kb-overrides.json
sheet.json
ai-enabled.json
cloudflared
public/uploads/
server.log
*.command
stats.json
ai-messages.jsonl
pancake-shops.json
page-shop-cache.json
ai-convs.json
page-product-cache.json
ai-created-orders.json
.env.vps
pancake-tokens.json
pancake-page-tokens.json
wa-auth/
wa-qr.png

# Lịch sử kịch bản (M02) + sổ cái page (M01) — kịch bản marketer viết + map shop, như kb-overrides.json
script-versions/
pages.json

# Trạng thái hội thoại (M05) + sổ template Botcake (M18) — dữ liệu thật, chỉ có trên VPS
conv-state.json
conv-state.json.tmp
botcake-templates.json

# L7 · báo cáo mổ (M15), sổ template chờ duyệt, hàng chờ tạo đơn (M14) — dữ liệu thật của VPS
miner-reports.jsonl
miner-state.json
template-candidates.json
template-learn-reports.jsonl
ai-order-queue.json

health-state.json

# rác của macOS
.DS_Store

# Bàn giao nội bộ — chứa .env, token, KB thật. TUYỆT ĐỐI không push lên git
ban-giao-noi-bo-*/

# Gói quy trình dây chuyền AI — tài liệu nội bộ, không push (bản cài đã nằm ở .claude/skills/)
goi-day-chuyen-ai*/
